export enum ACTVariants {
  UnknownOrAny,
  Fungible,
  NonFungible,
}

export enum ACTStates {
  Locked,
  Active,
  Tracked,
}

export enum ACTSystems {
  AbsoluteMonarchy,
  ConstitutionalMonarchy,
  Democracy,
}
